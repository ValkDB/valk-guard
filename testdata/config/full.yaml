format: json

exclude:
  - "vendor/*"
  - "*.gen.sql"

migration_paths:
  - "db/migrations"
  - "schema/**/*.sql"

rules:
  VG001:
    enabled: true
    severity: error
  VG002:
    enabled: false
    severity: warning
  VG003:
    severity: info
