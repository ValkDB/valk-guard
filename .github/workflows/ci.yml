name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          path: valk-guard

      - uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6.3.0
        with:
          go-version: '1.25.6'
          cache-dependency-path: valk-guard/go.sum

      - name: golangci-lint
        uses: golangci/golangci-lint-action@55c2c1448f86e01eaae002a5a3a9624417608d84 # v6.5.2
        with:
          version: v1.64.8
          install-mode: goinstall
          working-directory: valk-guard

      - name: go vet
        working-directory: valk-guard
        run: go vet ./...

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          path: valk-guard

      - uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6.3.0
        with:
          go-version: '1.25.6'
          cache-dependency-path: valk-guard/go.sum

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: '3.12'

      - name: Verify Python
        run: python3 --version

      - name: Run tests
        working-directory: valk-guard
        run: go test -race -coverprofile=coverage.out -covermode=atomic ./...

      - name: Print coverage
        working-directory: valk-guard
        run: go tool cover -func=coverage.out

      - name: Vulnerability check
        working-directory: valk-guard
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@v1.1.4
          govulncheck ./...

  self-scan:
    name: Self-scan
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          path: valk-guard

      - uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6.3.0
        with:
          go-version: '1.25.6'
          cache-dependency-path: valk-guard/go.sum

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: '3.12'

      - name: Build valk-guard
        working-directory: valk-guard
        run: go build -ldflags "-s -w -X main.version=${GITHUB_SHA}" -o valk-guard ./cmd/valk-guard/

      - name: Scan testdata
        working-directory: valk-guard
        run: |
          set +e
          ./valk-guard scan testdata/ --format json
          code=$?
          set -e
          if [ "$code" -gt 1 ]; then
            exit "$code"
          fi

  pr-review:
    name: PR Review (Valk Guard)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    needs: [test]
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          path: valk-guard
          fetch-depth: 0

      - uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6.3.0
        with:
          go-version: '1.25.6'
          cache-dependency-path: valk-guard/go.sum

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: '3.12'

      - name: Build valk-guard
        working-directory: valk-guard
        run: go build -ldflags "-s -w -X main.version=${GITHUB_SHA}" -o valk-guard ./cmd/valk-guard/

      - name: Initialize valk-guard JSON report
        working-directory: valk-guard
        run: echo '{"version":"1.0.0","findings":[],"summary":{"total":0}}' > valk-guard.json

      - name: Collect changed SQL-related files
        id: changed
        working-directory: valk-guard
        run: |
          base_sha="${{ github.event.pull_request.base.sha }}"
          head_sha="${{ github.event.pull_request.head.sha }}"

          files="$(git diff --name-only --diff-filter=ACMRT "$base_sha" "$head_sha" | grep -E '\.(sql|go|py)$' || true)"

          if [ -z "$files" ]; then
            echo "has_files=false" >> "$GITHUB_OUTPUT"
            echo "count=0" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          printf '%s\n' "$files" > changed-files.txt
          count="$(wc -l < changed-files.txt | tr -d ' ')"
          echo "has_files=true" >> "$GITHUB_OUTPUT"
          echo "count=$count" >> "$GITHUB_OUTPUT"
          echo "Changed files:"
          cat changed-files.txt

      - name: Run valk-guard on changed files
        if: steps.changed.outputs.has_files == 'true'
        working-directory: valk-guard
        run: |
          mapfile -t files < changed-files.txt

          set +e
          ./valk-guard scan "${files[@]}" --format json > valk-guard.json
          code=$?
          set -e

          # findings => code 1 (non-blocking for review comments)
          # failures => code 2+ (block because scanner/runtime/config issue)
          if [ "$code" -gt 1 ]; then
            exit "$code"
          fi

      - name: Upload valk-guard JSON report
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: valk-guard-pr-json-${{ github.event.pull_request.number }}
          path: valk-guard/valk-guard.json
          if-no-files-found: error

      - uses: reviewdog/action-setup@d8a7baabd7f3e8544ee4dbde3ee41d0011c3a93f # v1.5.0
        if: steps.changed.outputs.has_files == 'true'

      - name: Convert findings to reviewdog format
        if: steps.changed.outputs.has_files == 'true'
        working-directory: valk-guard
        run: |
          jq -cr '
            ((if type == "object" then .findings else . end) // [])[] |
            {
              source: {
                name: "valk-guard",
                url: "https://github.com/valkdb/valk-guard"
              },
              severity: (
                if .severity == "error" then "ERROR"
                elif .severity == "warning" then "WARNING"
                else "INFO"
                end
              ),
              code: { value: .rule_id },
              message: (.rule_id + ": " + .message),
              location: {
                path: .file,
                range: {
                  start: { line: .line, column: (.column // 1) },
                  end:   { line: .line, column: ((.column // 1) + 1) }
                }
              }
            }
          ' valk-guard.json > valk-guard.rdjsonl

      - name: Post PR review comments
        if: steps.changed.outputs.has_files == 'true'
        env:
          REVIEWDOG_GITHUB_API_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        working-directory: valk-guard
        run: |
          if [ ! -s valk-guard.rdjsonl ]; then
            echo "No valk-guard findings on changed files."
            exit 0
          fi

          reviewdog \
            -f=rdjsonl \
            -name="valk-guard" \
            -reporter=github-pr-review \
            -filter-mode=added \
            -fail-level=none \
            < valk-guard.rdjsonl

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test]
    strategy:
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          path: valk-guard

      - uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6.3.0
        with:
          go-version: '1.25.6'
          cache-dependency-path: valk-guard/go.sum

      - name: Build
        working-directory: valk-guard
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          ext=""
          if [ "$GOOS" = "windows" ]; then ext=".exe"; fi
          go build -ldflags "-s -w -X main.version=${GITHUB_SHA}" -o dist/valk-guard-${GOOS}-${GOARCH}${ext} ./cmd/valk-guard/

      - name: Upload artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: valk-guard-${{ matrix.goos }}-${{ matrix.goarch }}
          path: valk-guard/dist/
